{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build-debug",
			"command": "cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build . -- -j4",
			"options": {
				"cwd": "${workspaceFolder}/build"
			}
		}
	]
}
